# FortiGate Terraform boundary

Reserved for future declarative FortiGate resources where a reviewed provider supports the required behavior. Candidate scope includes network/policy objects; exact ownership is OPEN DECISION.

Review compatibility, adoption/import, policy ordering, drift, state sensitivity, least-privilege access, NAT/VIP behavior and management lockout recovery. Coordinate with `network/fortigate/`: bootstrap/recovery and steady-state tooling must not write the same objects concurrently.

Current content: responsibility documentation only. No provider, VLAN60, interface, DHCP scope, policy, zone or NAT/VIP configuration is implemented.
