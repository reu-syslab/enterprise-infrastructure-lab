# Proxmox Terraform boundary

Reserved for future approved Proxmox infrastructure lifecycle code, potentially guest provisioning and network attachments. No provider or resource ownership has been selected.

Review provider/version compatibility, credentials/permissions, state/backend, existing resource adoption, template lifecycle, VM IDs and capacity. Protect the existing VLAN-aware `vmbr0`, legacy bridge references and untagged templates until their migration is separately reviewed. Windows/Linux service configuration belongs to the reviewed OS tooling boundary, not an accidental overlapping owner.

Current content: responsibility documentation only. No VM, DC01, bridge, VLAN or template is created.
