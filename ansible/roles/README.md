# Future reusable roles

Reserve this directory for cohesive, reviewed roles with documented inputs, dependencies, privileges, idempotence expectations, verification and rollback. Potential areas are Linux baseline, DNS, Kerberos/SSSD, Windows configuration and Zabbix; none is selected or implemented here.

Use synthetic role examples only. Do not create empty operational role structures that imply readiness. The future role contract must distinguish private environment inputs from reusable defaults and avoid competing ownership with Terraform or PowerShell.
