# Data boundary

`example/` contains synthetic documentation samples only. No live environments, inventory, secrets or public-to-private mappings belong under this directory. Actual environment data is maintained outside the repository, including ignored paths. A future runtime-input schema is an OPEN DECISION; current samples do not define that interface.
